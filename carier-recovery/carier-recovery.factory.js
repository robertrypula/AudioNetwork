/*
usage

    var cr = CarierRecovery(samplePerPeriod);
    
    
    sampleLoop {
        cr.handleSample(sample);
        
        if (cr.carierAvailabe()) {
            cr.getCarier();
        } else {
            cr.
        }
    }

*/